package model.entities;

public class Bullet extends Missile {
    public Bullet(int x, int y, int speed, boolean fromPlayer, String[] framePaths) {
        super(x, y, speed, fromPlayer, framePaths);
    }
}
